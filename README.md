# generic-template

Base para novos repositórios da organização. Consulte
`docs-warehouse/templates/make/README.md` ao configurar o Makefile e defina o
identificador de serviço antes de habilitar `make env`.
